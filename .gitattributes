# Shell scripts run inside Linux containers (see crates/frontend/Dockerfile) —
# a CRLF line ending breaks the #!/bin/sh shebang there. Force LF
# regardless of core.autocrlf on a Windows checkout.
*.sh text eol=lf
